pipeline {
    agent any

    tools {
        nodejs "NodeJS234"
    }

    stages {
        stage ('Print NodeJS Version') {
            steps {
                sh 'node -v'
            }
        }
   }
}